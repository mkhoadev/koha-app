import React from "react";
import { SafeAreaView, ScrollView, View } from "react-native";

function HeathScreen() {
  return (
    <SafeAreaView>
      <ScrollView showsVerticalScrollIndicator={false}>
        <View></View>
      </ScrollView>
    </SafeAreaView>
  );
}

export default HeathScreen;
